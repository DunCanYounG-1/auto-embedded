# 行尾锁定：
# - Windows 批处理 .cmd/.bat 必须 CRLF，否则 cmd.exe 逐行误解析（实测会报 '_DIR'/'defined' 非命令）。
# - POSIX shell 与 bin/aemb 启动器必须 LF，否则 bash 报 bad interpreter。
# - 其余按 LF 归一，避免跨平台噪声。
* text=auto eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf
bin/aemb text eol=lf
*.sh text eol=lf

# ── GitHub Linguist 语言统计 ──
# templates/auto-embedded/tools/ 是从 embedded-dev 插件 bundled 进来的 21 个工具脚本（上游载荷，
# 非本仓库自研 CLI）；tests/ 为自测脚本。二者标 vendored，让语言条反映 auto-embedded 自研主体
# （TypeScript 脚手架 CLI + Python 运行时内核），而非被 bundled 的工具脚本体量淹没。
templates/auto-embedded/tools/** linguist-vendored
tests/** linguist-vendored
